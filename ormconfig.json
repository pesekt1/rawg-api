{
  "type": "mysql",
  "host": "localhost",
  "port": 3306,
  "username": "root",
  "password": "123456",
  "database": "rawgdb",
  "synchronize": true,
  "logging": true,
  "entities": ["models-sql/**/*.ts"],
  "migrations": ["migration/**/*.ts"],
  "subscribers": ["subscriber/**/*.ts"],
  "cli": {
    "entitiesDir": "models-sql",
    "migrationsDir": "migration",
    "subscribersDir": "subscriber"
  }
}
